public interface GestionPropriete {
    final public  static  int MAX_PROPRIETES = 100;
    public void afficherProprites();
    public boolean ajouter(Proprite p);
    public boolean supprime(Proprite p);
}
